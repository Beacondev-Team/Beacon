services:
  bot:
    build: .
    container_name: beacon
    volumes:
      - .:/src
    working_dir: /src
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      GITHUB_TOKEN: ${GITHUB_TOKEN}
    command: python main.py